apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: sumonskys/school-management-system:latest
          ports:
            - containerPort: 5000
          env:
            - name: ENVIRONMENT
              value: "production"
